import Typography from 'antd/es/typography';
import React from 'react';

const { Title } = Typography;

export const BusinessResources: React.FC = () => {
    return (
        <>
            <Title level={3}>In progress</Title>
        </>
    );
};
